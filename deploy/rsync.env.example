# Copy to deploy/rsync.env and fill in (deploy/rsync.env is gitignored).
# Used by deploy/rsync-to-vps.sh for local deploys from Git Bash / WSL / macOS / Linux.

VPS_HOST=66.116.239.195
VPS_USER=root
VPS_APP_DIR=/var/www/checkmockup
VPS_SSH_PORT=22
# Path to your private SSH key (optional if using ssh-agent / default key)
VPS_SSH_KEY=~/.ssh/dsbgroup_deploy
